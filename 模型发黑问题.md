  - 预览阶段（复制后鼠标跟随的模型）在 src/brush/doodad_brush.cpp 里 render_clipboard() 固定用白色渲染：
    queue_render(..., glm::vec3(1.f), 0)
    所以看起来总是正常。
  - 放到地图后，会在 Doodad::update()（src/base/doodad.ixx）重新从 SLK 读颜色：
      - doodad 用 vertr/vertg/vertb
      - destructible 用 colorr/colorg/colorb
  - 但 slk.data<float> 在字段不存在时返回 0（见 src/file_formats/slk.ixx），
  如果你要我修，我建议两个动作（都不大改动）：

  1. 当颜色字段缺失时默认 255（白色），避免被置黑。
  2. 预览也用真实颜色，避免“预览正常、落地变黑”的错觉。

  如果你要我修，我建议两个动作（都不大改动）：


  1. 当颜色字段缺失时默认 255（白色），避免被置黑。
  2. 预览也用真实颜色，避免“预览正常、落地变黑”的错觉。